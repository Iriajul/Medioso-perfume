# ─── Mad Perfume Staging Overrides ─────────────────────────────────────────
env: staging
website_domain: "bpmstudio.pt"
api_host: "staging-api.{{ website_domain }}"
admin_host: "staging-admin.{{ website_domain }}"
# Staging deploys the mutable :staging image tag unless IMAGE_TAG is set.
image_tag: "{{ lookup('env', 'IMAGE_TAG') | default('staging', true) }}"
certbot_email: "syedriajul420islam@gmail.com"
