# Copy to .env.production.frontend (gitignored).
# NEXT_PUBLIC_* values are baked in at build time (CI passes them as build-args);
# this file only covers server-side runtime values.
NEXT_PUBLIC_API_URL=https://api.bpmstudio.pt
