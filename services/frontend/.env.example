# Copy to .env.local for `npm run dev` outside Docker.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Server-side (server actions) API base; defaults to NEXT_PUBLIC_API_URL.
# API_URL=http://backend:8000
